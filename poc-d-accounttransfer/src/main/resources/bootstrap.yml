spring:
  application:
    name: poc-d-accounttransfer
  config:
    name: poc-d-accounttransfer
  cloud:
    config:
      uri: http://config:8888
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user
